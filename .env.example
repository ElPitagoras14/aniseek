# Docker: Prefix for Docker container names.
COMPOSE_PROJECT_NAME=aniseek

# Ports: External ports exposed by the services.
WEB_PORT=4000
API_PORT=4002

# Optional Port Assigments (Used if running services locally or for external access)
POSTGRES_PORT=4003
REDIS_PORT=4004
FLOWER_PORT=4005

# API & Web Configuration
# DEV only: URL that the Web server will use to connect to the API server (internal communication).
API_URL=http://192.168.1.71:4002

# NEXT_PUBLIC_API_URL: URL that the client (browser) will use to connect to the API.
# It must be accessible from other devices on the network.
# Example: http://192.168.1.100:4002 or https://api.midominio.com
NEXT_PUBLIC_API_URL=https://api.midominio.com

# NEXTAUTH_URL: URL that NextAuth uses for authentication callbacks.
# This must be accessible from the client (browser).
# Do NOT use localhost unless only this machine will access it.
# - For other devices on the local network: use the local IP of the host machine (e.g., http://192.168.1.100:4000)
# - For production: use the public domain or server IP (e.g., https://midominio.com)
NEXTAUTH_URL=https://midominio.com

# AUTH_SECRET: Secret used by NextAuth for session encryption. Must be a long, random string.
AUTH_SECRET=supersecretprodkey

# Databases
# POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB: Credentials for PostgreSQL database.
POSTGRES_USER=produser
POSTGRES_PASSWORD=prodpass
POSTGRES_DB=proddb
# DEV only: Direct connection URLs for local development outside Docker.
POSTGRES_URL=postgresql://produser:prodpass@192.168.1.71:4003/proddb
REDIS_URL=redis://192.168.1.71:4004


# Backend Configuration
# SECRET_KEY: Secret key used for JWT signing in the API. Must be a long, random string.
SECRET_KEY=prodsecretkey
# ALGORITHM: Algorithm used for JWT signing (e.g., HS256).
ALGORITHM=HS256
# ADMIN_USER, ADMIN_PASS: Credentials for the initial administrator user created on startup.
ADMIN_USER=admin
ADMIN_PASS=admin123
# APP_LOG_PATH: Path inside the container where logs will be stored (if volume is mounted). Leave empty if not mounting logs.
APP_LOG_PATH=

# Animes folder (Linux server assumed)
# DOCKER_DATA_FOLDER: Host path where persistent data (like downloaded animes) will be stored.
DOCKER_DATA_FOLDER=/srv/data
# ANIMES_FOLDER: Internal path inside the containers where animes are stored. Must match the volume mount defined in docker-compose.yaml.
ANIMES_FOLDER=/animes

# Dramatiq Configuration
# MAX_DOWNLOAD_RETRIES: Maximum number of times a download task will be retried upon failure.
MAX_DOWNLOAD_RETRIES=5
# RETRY_DOWNLOAD_INTERVAL: Interval (in seconds) between download retries.
RETRY_DOWNLOAD_INTERVAL=15
# DRAMATIQ_PROCESSES: Number of processes for the Dramatiq worker.
DRAMATIQ_PROCESSES=2
# DRAMATIQ_THREADS: Number of threads per process for the Dramatiq worker.
DRAMATIQ_THREADS=4
# WORKER_LOG_PATH: Path inside the container where logs will be stored (if volume is mounted). Leave empty if not mounting logs.
WORKER_LOG_PATH=

# Tokens Configuration
# ACCESS_TOKEN_EXP_MIN: Access token expiration time in minutes.
ACCESS_TOKEN_EXP_MIN=30
# REFRESH_TOKEN_EXP_DAY: Refresh token expiration time in days.
REFRESH_TOKEN_EXP_DAY=10

# Remote (Optional, Personal Use)
# WEB_REMOTE_PORT: Port for the remote web instance.
WEB_REMOTE_PORT=4001
# NEXT_PUBLIC_API_URL_REMOTE: Public API URL for the remote instance.
NEXT_PUBLIC_API_URL_REMOTE=https://remote-api.midominio.com
# NEXTAUTH_URL_REMOTE: NextAuth URL for the remote instance.
NEXTAUTH_URL_REMOTE=https://remote-midominio.com
